[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "secwatch"
version = "0.1.0"
description = "Defensive security-check scoring utilities"
requires-python = ">=3.9"
[tool.pytest.ini_options]
pythonpath = ["src"]
